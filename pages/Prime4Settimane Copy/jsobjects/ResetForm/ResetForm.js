export default {
  resetForm1() {
    // Rating da 1 a 17
    for (let i = 1; i <= 17; i++) {
      resetWidget(`Rating${i}`);
      resetWidget(`Input${i}`);
    }
  },

  resetForm2() {
    // Rating da 18 a 28
    for (let i = 18; i <= 28; i++) {
      resetWidget(`Rating${i}`);
      resetWidget(`Input${i}`);
    }
  },
	
	  resetForm3() {
    // Rating da 29 a 36
    for (let i = 29; i <= 36; i++) {
      resetWidget(`Rating${i}`);
      resetWidget(`Input${i}`);
    }
  },
	
	  resetForm4() {
    // Rating da 37 a 46
    for (let i = 37; i <= 46; i++) {
      resetWidget(`Rating${i}`);
      resetWidget(`Input${i}`);
    }
  },
	
	  resetForm5() {
    // Rating da 47 a 52
    for (let i = 47; i <= 52; i++) {
      resetWidget(`Rating${i}`);
      resetWidget(`Input${i}`);
    }
  }
}
